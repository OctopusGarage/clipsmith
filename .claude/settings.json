{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); [ -z \"$f\" ] && exit 0; case \"$f\" in *.py) cd \"$CLAUDE_PROJECT_DIR\" && if uv run ruff --version >/dev/null 2>&1; then uv run ruff format --check \"$f\" 1>&2; fi ;; *.sh) bash -n \"$f\" 1>&2 ;; esac"
          }
        ]
      }
    ]
  }
}
