version: 1
interface:
  display_name: Clipsmith X Capture
  description: Export X/Twitter post text and media through the existing browser automation runner, then normalize post.md and assets into a Clipsmith bundle.
  default_prompt: Use this skill for X or Twitter post URLs. Invoke the existing scripts/run.ts runner, never substitute custom browser automation, reuse the authenticated Chrome CDP session, preserve target post text and media, resolve t.co links as required, and normalize output into an x Clipsmith bundle before validation.
